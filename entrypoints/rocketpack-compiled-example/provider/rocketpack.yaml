version: 1
name: rocketpack-provider-schema

sources:
  - base_dir: rpfs
    includes:
      - "**/*.rpf"

generators:
  - id: rust
    plugin: rocketpack-rust
    options:
      output_dir: rust/gen/src
      module_name: rocketpack
