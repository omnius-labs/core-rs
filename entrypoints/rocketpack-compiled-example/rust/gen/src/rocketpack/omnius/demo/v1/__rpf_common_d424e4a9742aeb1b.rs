// @generated by rocketpack-compiler
#[allow(dead_code)]
#[allow(clippy::all)]
#[derive(Debug, Clone, PartialEq)]
pub enum Status {
    Success,
    Failed,
}

impl omnius_core_rocketpack::RocketPackStruct for Status {
    fn validate(value: &Self) -> std::result::Result<(), omnius_core_rocketpack::RocketPackEncoderError> {
        match value {
            Self::Success => {}
            Self::Failed => {}
        }
        Ok(())
    }

    fn pack(encoder: &mut impl omnius_core_rocketpack::RocketPackEncoder, value: &Self) -> std::result::Result<(), omnius_core_rocketpack::RocketPackEncoderError> {
        Self::validate(value)?;
        encoder.write_map(1)?;

        match value {
            Self::Success => {
                encoder.write_u64(1)?;
                encoder.write_map(0)?;
            }
            Self::Failed => {
                encoder.write_u64(2)?;
                encoder.write_map(0)?;
            }
        }

        Ok(())
    }

    fn unpack(decoder: &mut impl omnius_core_rocketpack::RocketPackDecoder) -> std::result::Result<Self, omnius_core_rocketpack::RocketPackDecoderError>
    where
        Self: Sized,
    {
        let mut result: Option<Self> = None;
        let count = decoder.read_map()?;

        for _ in 0..count {
            match decoder.read_u64()? {
                1 => {
                    let __inner_count_0 = decoder.read_map()?;
                    for _ in 0..__inner_count_0 {
                        let _ = decoder.read_u64()?;
                        decoder.skip_field()?;
                    }
                    result = Some(Self::Success);
                }
                2 => {
                    let __inner_count_1 = decoder.read_map()?;
                    for _ in 0..__inner_count_1 {
                        let _ = decoder.read_u64()?;
                        decoder.skip_field()?;
                    }
                    result = Some(Self::Failed);
                }
                _ => decoder.skip_field()?,
            }
        }

        result.ok_or(omnius_core_rocketpack::RocketPackDecoderError::Other("missing enum variant"))
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct SimpleMessage {
    pub bool_field: Option<bool>,
}

impl omnius_core_rocketpack::RocketPackStruct for SimpleMessage {
    fn validate(_value: &Self) -> std::result::Result<(), omnius_core_rocketpack::RocketPackEncoderError> {
        Ok(())
    }

    fn pack(encoder: &mut impl omnius_core_rocketpack::RocketPackEncoder, value: &Self) -> std::result::Result<(), omnius_core_rocketpack::RocketPackEncoderError> {
        Self::validate(value)?;
        let mut count = 0;
        if value.bool_field.is_some() {
            count += 1;
        }
        encoder.write_map(count)?;
        if let Some(bool_field) = &value.bool_field {
            encoder.write_u64(1)?;
            encoder.write_bool(*(bool_field))?;
        }
        Ok(())
    }

    fn unpack(decoder: &mut impl omnius_core_rocketpack::RocketPackDecoder) -> std::result::Result<Self, omnius_core_rocketpack::RocketPackDecoderError>
    where
        Self: Sized,
    {
        let mut bool_field: Option<bool> = None;
        let count = decoder.read_map()?;

        for _ in 0..count {
            match decoder.read_u64()? {
                1 => {
                    bool_field = Some(decoder.read_bool()?);
                }
                _ => decoder.skip_field()?,
            }
        }

        Ok(Self { bool_field: bool_field })
    }
}

pub type ByteList = Vec<Vec<u8>>;
